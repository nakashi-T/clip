<div class="current-wrapper">
  <% posts.each do |post| %>
    <div class="post-image">
      <div class="inner">
        <% if post.image? %>
          <%= link_to image_tag(post.image.url), post_path(post) %>
        <% end %>
      </div>
    </div>
  <% end %>
</div>